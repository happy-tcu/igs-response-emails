// Copy this file to `config.js` and fill in your project values.
// config.js is gitignored — never commit real keys.
// Same Supabase project as the grading console; the anon key is safe in the
// browser because RLS + admin-only RPCs enforce access server-side.
window.ISOMO_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
