// Copy this file to `config.js` and fill in your project values.
// config.js is gitignored — never commit real keys.
// The anon key is safe to expose in the browser; RLS + the security-definer
// RPCs are what actually protect the data.
window.ISOMO_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
